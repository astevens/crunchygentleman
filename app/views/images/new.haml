= form_for(@image, url(:images, :create), :multipart => true) do |f|
  %ul
    %li
      = f.label :description
      = f.text_field :description
    %li
      = f.label :file
      = f.file_field :file
      = f.hidden_field :file_cache
  = f.submit("Save")